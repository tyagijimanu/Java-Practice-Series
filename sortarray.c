//ye sorting wala nhi finding the element wala program h
void main
{
  int a[10], i, se, F=0;
  clrscr();
  printf("\n Enter array elemetns:");

 //taking the elements of the array through input function
  for(i=0; i<=9; i++)
  {
    scanf("%d", &a[i]);
  }

  //enter the elements that is to be found 
  printf("\n Enter se");
  scanf("%d", &se);

  for(i=0; i<=9; i++)
  {
    if(se==a[i])
    {
      
    }
  }
  )
}